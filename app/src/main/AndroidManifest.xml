<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maxdexter.mynote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity android:name=".NotePagerActivity" android:parentActivityName=".MainActivity" android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".DetailActivity" android:windowSoftInputMode="adjustResize"/>
        <provider
            android:authorities="com.maxdexter.mynote.fileprovider"
            android:name="android.support.v4.content.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file"/>
        </provider>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
<!--    Чтобы Activity подстраивалась под изменение положения клавиатуры, -->
<!--    необходимо указать в манифесте для Ваших активити аттрибут: android:windowSoftInputMode="adjustResize".-->